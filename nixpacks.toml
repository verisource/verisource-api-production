[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "ffmpeg"]

[phases.install]
cmds = ["npm install", "pip install pyannote.audio torch torchaudio --quiet"]

[start]
cmd = "node index.js"
